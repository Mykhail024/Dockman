# Certificates

Place your certificates here or use `generate_certs.sh`.

Required files:
- `ca.crt` — Certificate Authority certificate
- `server.crt` — Server SSL/TLS certificate
- `server.key` — Private key for the server certificate
